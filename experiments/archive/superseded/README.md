# Superseded experiment inputs

These YAML files are retained so old results remain understandable. Each has
a newer maintained replacement in the parent `experiments/` directory or in
the current release-gate mapping.

They are not discovered or selected by the maintained GPU launchers. Do not
copy one back into the active directory; add a new explicitly named gate and
update `submit_gpu_release_gates.py` instead.

