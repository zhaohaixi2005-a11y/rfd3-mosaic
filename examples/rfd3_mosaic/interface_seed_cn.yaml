schema_version: 1
name: c5-interface-exact

topology:
  kind: interface_seed
  config: ../../configs/rfd3_mosaic/cyclic/lhd101_c5.yaml
  pose_candidate_manifest: /path/to/c5/candidate/manifest.json
  linker_length: 85

sampling:
  preset: exact_mosaic
  timesteps: 200
  seed: 42

resources:
  profile: h100

output:
  root: /path/to/rfd3-mosaic-runs
  campaign: interface-seed
